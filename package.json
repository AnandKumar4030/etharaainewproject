{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Root package.json for Railway deployment",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npm run prisma:generate --prefix backend",
    "start": "npm run prisma:push --prefix backend && npm start --prefix backend"
  }
}
